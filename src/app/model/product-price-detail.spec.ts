import { ProductPriceDetail } from './product-price-detail';

describe('ProductPriceDetail', () => {
  it('should create an instance', () => {
    expect(new ProductPriceDetail()).toBeTruthy();
  });
});
